nda_martian = {
    usage = game
    selection_behavior = weighted_random

    nda_martian_skin = {
        dna_modifiers = {

            ### Skin Color ###
            morph = {
                mode = add
                gene = NDA_gene_skin_color
                template = blue_skin
                range = { 0 1 } # For the randomness to work correctly
            }
        }

        weight = {
            base = 0
            modifier = {
                add = 1000

                OR = {
                    scope:character ?= {
                        has_culture = cu:nda_cul_aresian
                    }
                    scope:pop ?= {
                        has_pop_culture = nda_cul_aresian
                    }
                }
            }
        }
    }
}

nda_martian_robe = {
    usage = game
	selection_behavior = weighted_random
	fallback = no_martian_robe

    no_martian_robe = {
        # no stuff needed
        weight = {
            base = 0
        }       
    }

    martian_robe= {
        dna_modifiers = {
            # add cult robes
            accessory = {
                mode = add
                gene = nda_top_layer
                template = martian_top_layer
                range = { 1 1 } # For the randomness to work correctly
            }
            accessory = {
                mode = replace
                gene = outfits
                template = peasant_outfits
                range = { 0 1 } # For the randomness to work correctly
            }
        }

        weight = {
            base = 0
            modifier = {
                add = 500
                scope:character ?= {
					has_culture = cu:nda_cul_aresian
                    is_in_exile_pool = no # stop error spam on viewing exiles - need to clean the exile pool of coven leaders?
                    age > 16
                }
            }
        }
    }  
}
