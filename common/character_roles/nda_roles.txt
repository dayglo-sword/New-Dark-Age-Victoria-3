character_role_nda_cultist = {
	texture = "gfx/interface/icons/character_role_icons/nda_cultist.dds"
    type = other       # Archetype for this role, valid archetypes are politician/general/admiral/executive/agitator/ruler/heir/magnate. Roles do not need to have archetypes if they are fully controlled by script.
    priority = 5            # Used to select the primary title for a character, higher priority gets selected over lower

    #auto_assigned = yes      # If this is set to yes, then all characters of this role's archetype will receive this role when created
   
    #should_use_title_in_name = yes   # If set to yes, characters with this role as primary will have its title in their titled name ('General Cupcakes Muffin III')
    title_format = custom            # Determines how titles are formatted, from_name/ruler/heir/custom, defaults to from_name. If set to custom, a customizable localization with the format key_custom_loc (example: character_role_politician_custom_loc) must be added to the customizable loc database.
    title_preposition = "PREP_IN"       # Determines the preposition when title is shown in country context (ie, 'Ruler in France'), uses define (english default: 'in') if not set
     
    career_length = { 30 50 } # limit length to remove older ones - if has other role then auto reassign

    # Applied to characters holding this role
    character_modifier = {
        character_prominence_add = 10
    }
}

### Non-Eldritch

character_role_nda_investigator = {
    texture = "gfx/interface/icons/character_role_icons/nda_investigator.dds"
    type = other       # Archetype for this role, valid archetypes are politician/general/admiral/executive/agitator/ruler/heir/magnate. Roles do not need to have archetypes if they are fully controlled by script.
    priority = 5            # Used to select the primary title for a character, higher priority gets selected over lower

    #auto_assigned = yes      # If this is set to yes, then all characters of this role's archetype will receive this role when created
   
    #should_use_title_in_name = yes   # If set to yes, characters with this role as primary will have its title in their titled name ('General Cupcakes Muffin III')
    title_format = from_name            # Determines how titles are formatted, from_name/ruler/heir/custom, defaults to from_name. If set to custom, a customizable localization with the format key_custom_loc (example: character_role_politician_custom_loc) must be added to the customizable loc database.
    title_preposition = "PREP_IN"       # Determines the preposition when title is shown in country context (ie, 'Ruler in France'), uses define (english default: 'in') if not set
   
    # Applied to characters holding this role
    character_modifier = {
        character_prominence_add = 10
    }
}

character_role_nda_mason = {
    texture = "gfx/interface/icons/character_role_icons/nda_masonic.dds"
    type = other       # Archetype for this role, valid archetypes are politician/general/admiral/executive/agitator/ruler/heir/magnate. Roles do not need to have archetypes if they are fully controlled by script.
    priority = 5            # Used to select the primary title for a character, higher priority gets selected over lower

    #auto_assigned = yes      # If this is set to yes, then all characters of this role's archetype will receive this role when created
   
    #should_use_title_in_name = yes   # If set to yes, characters with this role as primary will have its title in their titled name ('General Cupcakes Muffin III')
    title_format = custom            # Determines how titles are formatted, from_name/ruler/heir/custom, defaults to from_name. If set to custom, a customizable localization with the format key_custom_loc (example: character_role_politician_custom_loc) must be added to the customizable loc database.
    title_preposition = "PREP_IN"       # Determines the preposition when title is shown in country context (ie, 'Ruler in France'), uses define (english default: 'in') if not set
 
    career_length = { 30 50 } # limit length to remove older ones - if has other role then auto reassign
    
    # Applied to characters holding this role
    character_modifier = {
        character_prominence_add = 10
    }
}