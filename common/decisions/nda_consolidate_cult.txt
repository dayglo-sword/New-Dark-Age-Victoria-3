# nda_consolidate_cult = { # try and clean up minor pops in country to try and improve performance
# 	is_shown = {

# 		has_variable = nda_cultists_in_open
# 		country_has_primary_culture = cu:nda_cul_cultist
		
# 	}

# 	possible = {
# 		custom_tooltip = {
# 			text = nda_revoke_cult_failure_tooltip
# 			has_variable = nda_cultists_in_open
# 		}
# 		NOT = { has_variable = nda_consolidate_cult_cooldown }
# 		country_has_primary_culture = cu:nda_cul_cultist

# 		custom_tooltip = {
# 			text = nda_revoke_cult_failure_tooltip
# 			any_scope_state = {
# 				save_temporary_scope_as = current_state
# 				any_scope_culture = {
# 					save_temporary_scope_as = current_culture
# 					scope:current_state  = {
# 						culture_percent_state = {
# 						target = scope:current_culture
# 						value < 0.01
# 						}
# 					}				
# 				}
# 			}			
# 		}

# 	}
    	
# 	when_taken = {

# 		debug_log = "Consolidate Minor Cultures to Cult : [THIS.GetCountry.GetNameNoFormatting]"
# 		debug_log_scopes = yes

# 		hidden_effect = {
# 			set_variable = {
# 				name = nda_consolidate_cult_cooldown
# 				days = 10
# 			}			
# 		}

# 		every_scope_state = {
# 			##save_temporary_scope_as  = current_state
# 			every_scope_culture = {
# 				limit = {
# 					NOT = { is_primary_culture_of = ROOT }
# 				}
# 				# limit = { 
# 				# 	NOT = { culture = cu:nda_cul_cultist } 
# 				# 	save_temporary_scope_as = current_culture
# 				# 	PREV = { #scope:current_state 
# 				# 		culture_percent_state = {
# 				# 			target = scope:current_culture
# 				# 			value < 0.01
# 				# 		}
# 				# 	}				  
# 				# }
# 				save_temporary_scope_as = current_culture
# 				IF = {
# 					limit = {
# 						PREV = { #scope:current_state 
# 							culture_percent_state = {
# 								target = THIS
# 								value < 0.01
# 							}
# 						}
# 					}

# 					PREV  = { #scope:current_state
# 						every_scope_pop = {
# 							limit = { 
# 								culture = scope:current_culture 
# 								NOT = { culture = cu:nda_cul_cultist } 
# 								NOT = { religion = cu:nda_rel_cultist } 
# 							}
# 							change_pop_religion = { target = rel:nda_rel_cultist value = 1 }
# 							change_pop_culture = { target = cu:nda_cul_cultist value = 1 }
# 						}
# 					}

# 				}

# 			}
# 		}
# 	}

# 	ai_chance = { 
# 		base = 50  # up chance to speed up breakdown	
# 	}

# }
